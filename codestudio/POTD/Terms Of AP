#include <vector>

vector < int > termsOfAP(int x) {
    
    vector < int > res;
    int got = 0;
    int curr = 5;

    while (got != x) {
   
        if (curr % 4 != 0) {
            res.push_back(curr);
            got++;
        }
        curr += 3;
    }
    return res;
}
